{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "conductor.schema.json",
  "title": "Conductor Configuration",
  "description": "Schema for conductor.json — the central configuration file for the Copilot conductor system.",
  "type": "object",
  "required": ["models", "agent"],
  "additionalProperties": false,
  "properties": {
    "trust": {
      "type": "object",
      "description": "Trust configuration for filesystem access.",
      "required": ["trusted_directories"],
      "additionalProperties": false,
      "properties": {
        "trusted_directories": {
          "type": "array",
          "description": "List of absolute directory paths that agents are permitted to operate in.",
          "items": {
            "type": "string",
            "description": "Absolute path to a trusted directory."
          },
          "minItems": 1,
          "uniqueItems": true
        }
      }
    },
    "models": {
      "type": "object",
      "description": "Model configuration for the conductor system.",
      "required": ["authorized_models", "default_model"],
      "additionalProperties": false,
      "properties": {
        "authorized_models": {
          "type": "array",
          "description": "List of model identifiers that agents are permitted to use.",
          "items": {
            "type": "string",
            "description": "Model identifier (e.g. 'claude-opus-4.6', 'gpt-4o')."
          },
          "minItems": 1,
          "uniqueItems": true
        },
        "default_model": {
          "type": "string",
          "description": "The default model used when an agent does not specify its own. Must be present in authorized_models."
        }
      }
    },
    "agent": {
      "type": "object",
      "description": "Agent definitions. Each key is a role identifier (e.g. 'conductor', 'developer'). The 'conductor' role is required.",
      "required": ["conductor"],
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*$",
        "description": "Role keys must be lowercase alphanumeric with optional hyphens."
      },
      "properties": {
        "conductor": {
          "$ref": "#/$defs/conductorAgent"
        }
      },
      "additionalProperties": {
        "$ref": "#/$defs/agent"
      }
    }
  },
  "$defs": {
    "agent": {
      "type": "object",
      "description": "Configuration for a non-conductor agent.",
      "required": [],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "Display name for this agent. Defaults to the role key if absent."
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of the agent's purpose. Used to generate the agent file and its 'Use when:' discovery description."
        },
        "active": {
          "type": "boolean",
          "description": "Whether the agent is active for this session. Agents without this key or with active=false are skipped.",
          "default": false
        },
        "model": {
          "type": "string",
          "description": "Optional model override for this agent. Falls back to models.default_model if absent."
        },
        "sidekick": {
          "type": "boolean",
          "description": "If true, this agent is the designated sidekick — always available for development tasks. Only one agent should have sidekick=true.",
          "default": false
        },
        "has_cron_jobs": {
          "type": "boolean",
          "description": "If true, a cron file at .github/agents/cron/<role>.cron is expected and will be created if absent.",
          "default": false
        },
        "scope": {
          "type": "string",
          "description": "Operational scope for this agent. 'system' agents are global; 'project' agents are scoped to a specific project workspace.",
          "enum": ["system", "project"]
        },
        "instructions": {
          "type": "string",
          "description": "Custom instructions for the agent to follow during execution. Used for agent-specific behavioral guidelines."
        },
        "instances": {
          "$ref": "#/$defs/instancePolicy"
        }
      }
    },
    "conductorAgent": {
      "type": "object",
      "description": "Configuration for the conductor agent. Same fields as a regular agent but always treated as active.",
      "required": ["name"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "Display name for the conductor agent (exposed as CONDUCTOR_AGENT_NAME)."
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of the conductor's role."
        },
        "active": {
          "type": "boolean",
          "description": "Always treated as true for the conductor. Included for consistency.",
          "default": true
        },
        "model": {
          "type": "string",
          "description": "Optional model override. Falls back to models.default_model if absent."
        },
        "has_cron_jobs": {
          "type": "boolean",
          "description": "If true, a cron file at .github/agents/cron/conductor.cron is expected.",
          "default": false
        },
        "scope": {
          "type": "string",
          "description": "Operational scope for the conductor. 'system' agents are global; 'project' agents are scoped to a specific project workspace.",
          "enum": ["system", "project"]
        },
        "instances": {
          "$ref": "#/$defs/instancePolicy"
        }
      }
    },
    "instancePolicy": {
      "type": "object",
      "description": "Concurrency policy: minimum and optional maximum number of simultaneous agent instances.",
      "additionalProperties": false,
      "properties": {
        "min": {
          "type": "integer",
          "description": "Minimum number of instances that must be running.",
          "minimum": 1,
          "default": 1
        },
        "max": {
          "type": "integer",
          "description": "Maximum number of concurrent instances allowed. Omit for no upper limit.",
          "minimum": 1
        }
      }
    }
  }
}
